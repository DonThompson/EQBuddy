<Window x:Class="EQBuddy.MezChipsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EQBuddy Mez Chips" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <!-- Who's asleep and for how long. Deliberately its own stack, separate from the
         spawn chips (David's call): mez chips live next to the fight, spawn chips are
         ambient camp furniture — they get parked in different places. -->
    <StackPanel x:Name="ChipsPanel"/>
</Window>
