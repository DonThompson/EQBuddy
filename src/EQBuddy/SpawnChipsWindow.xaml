<Window x:Class="EQBuddy.SpawnChipsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EQBuddy Spawn Chips" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False"
        WindowStartupLocation="Manual">
    <!-- The minimized face of spawn tracking: one small chicklet per running countdown,
         stacked vertically, the whole stack draggable as one. No chrome beyond the
         chicklets themselves — this sits over the game. -->
    <StackPanel x:Name="ChipsPanel"/>
</Window>
